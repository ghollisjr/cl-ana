makeres-macro is a free (GPL) Common Lisp graph transformation for use
with makeres.

makeres-macro allows the definition of macros which will be expanded
in the body of a defres in the transformation pipeline; this allows
dependency checking for e.g. (res) forms which would otherwise be
hidden.
